{
  "name": "afk-cats",
  "version": "0.1.0",
  "private": true,
  "description": "Discord bot and AFK sign pages, running as one Cloudflare Worker.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node scripts/register-commands.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
