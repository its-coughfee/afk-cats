# ad30b96 — Build chat overview: two builds shipped and the deploy walkthrough driven to its end, with the product reaching real use for the first time and immediately raising a question about what it is

One /next run and one /rescan, closing here. The run's own work is recorded per
item: [bot-icon], [circular-hold-deadlock], [afk-wire-up], and the two red-flagged
items walked alongside the last of those. This entry carries what belongs to the
chat rather than to any item.

**The session's shape is worth naming, because it is unusual.** Two builds went
through cleanly. The third item was a walkthrough, and driving it put the product
in front of its user for the first time — which is where nearly everything this
session learned came from. Seven captures were filed, and only one of them could
have been found by reading the code.

**The one that matters most is [sign-cannot-show-over-focused-window].** You said
the sign "just opens a page which isn't going to work when I have the stream focused
on Claude". SPEC assumes you keep the sign in a browser tab and share that tab, and
the private per-person address and the full-screen layout all follow from that
sentence. You stream a focused window instead, so the sign is invisible for exactly
as long as it would be worth showing. That is a hole in what SPEC assumes rather
than a bug in what was built, and it is filed as a SPEC conversation rather than a
fix. No SPEC sentence was written this session, because nothing was decided —
raising the question is not settling it.

## Also in this chat

**Corrections you gave, both of which changed how the work was done.**

At step 2 of the walkthrough you said plainly that you do not know the terminal, and
that pasting the command as written would have run it in the wrong folder — which
is what then happened on the first attempt. The step had said only that it "needs a
terminal, sitting in the project folder". The opening moves were supplied live and
the gap filed as [walkthrough-assumes-terminal-knowledge]. Later in the same step
you said "i don't understand" to a message asking you to substitute values into a
command; that was re-driven by taking the public Application ID into the chat and
handing back the finished line, leaving you to build only the part that had to stay
secret. Both are now in this project's memory as
`terminal-is-unfamiliar-ground`, so the next session does not
have to learn it from you again.

**Two secrets reached the transcript through screenshots**, and both were repaired
the same session — see the two red-flagged entries. The general fix belongs to the
method and was filed by the re-scan as [screenshot-defeats-secret-keeping]: a step
that deliberately keeps something out of the transcript should say, in the step
itself, not to screenshot the window while it is on screen.

**Work Claude did that no item asked for.** `npm install` was run as step 1 of the
walkthrough, which created `package-lock.json` — a real project file, folded into
this commit rather than treated as debris. The KV namespace was created and both
`wrangler.toml` values written by Claude with your approval and a one-file scope
addition, rather than handed over, because copying a 32-character random id between
two windows by hand is the kind of task worth not handing over. The deploy was put
to you separately before being run, since it makes the Worker publicly reachable.

**A queue-lint advisory was raised and deliberately left.** The heading of
[register-script-bash-syntax] starts with an article, which the outline view
truncates badly. A build cannot hand-edit the queue, so it stands for planning to
fix; the lint will keep saying so.

**A false alarm worth recording so it is not re-investigated.** The sign appeared
not to respond to `/afk`. The Worker's own state endpoint was queried directly and
was correct; the page polls every fifteen seconds and had been looked at inside that
window. Nothing was wrong. The item's own reasoning had already predicted a version
of this — the storage can serve reads up to a minute stale — which is why
[sign-page-browser-check] is written to measure the two flip timings rather than
assume them.

**Wind-down re-scan:** covered by the rescan just run — nothing happened between it
and the close but the close itself.

**Staleness sweep:** clean. The remaining items' references to [sign-page-eyeball]
and [kv-read-staleness] are citations of retired work and correct as written, and
[bot-icon-upload]'s step 3 now points at a file that exists.

Advisory: filed — [forward-advisory]
