# [HASH] — [discord-issue-closed-as-not-a-bug] processed and deleted: the request to Discord was closed within the hour for being on the wrong form, so it was re-posted as a Discussion and the sent register corrected, leaving nothing in the item to build

This capture was filed by a second planning chat the user had accidentally opened
on the same project. That chat was archived once found; its one capture was
already safely in the file, so nothing was lost. Its claim was checked against
GitHub rather than taken on trust, and it was accurate.

What happened. The issue asking Discord for a way to draw over a Go Live stream
was closed the same day by a repository contributor, whose entire comment was that
it is not a bug report and that a feature request belongs in a Discussion. So it
was turned away over which form it arrived on rather than over what it asked for,
and nobody who could act on it read it.

Both of the item's open threads were closed in the session, which is what left
nothing to build. The argument was re-posted in the API Feature Requests & Ideas
category the contributor named, with the user's approval on the exact text; the
new text acknowledges the closed issue and distinguishes itself from an existing
2021 request for bots to broadcast, which is a related but different ask. And
`INBOX/sent.md` was corrected: the issue line now records the closure and its
reason, and the Discussion is recorded beside it as open and watched.

What was checked before recommending anything, so a later session does not repeat
it. Discussions are enabled on that repository and the category exists, so the
contributor was pointing somewhere real. The 2021 request has sat there with 35
upvotes and no reply, which is the honest measure of what re-filing buys: it puts
the argument on record where it belongs and will not produce an answer soon.
Commenting on that older request was weighed and rejected — the two asks differ
enough that it would read as off-topic and be buried.

**Queue changes:** [discord-issue-closed-as-not-a-bug] deleted from Unprocessed
once both its open threads were closed in-session.

**Work processed:** deleted — [discord-issue-closed-as-not-a-bug].

**Advisory:** filed — [hold-scope-unanswered]
