# LOG Index

One-line summaries of each session. Newest first. Each line names the session's
full entry file in this folder.

- [HASH] — Planning chat overview: three items processed and all three deleted, Unprocessed cleared to none then to one from the close's re-scan, Processed unchanged at four with one cleared; the session's finding is that [afk-accounts]'s walkthrough never reached a run because `generate_build_view.py` matches the label only when a full stop or colon follows it immediately and the two headings carried an em dash first, fixed to a single `Walkthrough:` and verified against a scratch copy; the tooling fault was messaged to the project the plugin is developed in; records the correction of an earlier wrong claim that the build run had misread the item, and the `BUILD-VIEW.md` written into the project root and removed → 2026-08-26-plan.md
- [HASH] — [afk-accounts-walkthrough] deleted: the seven steps it asked for were present in every saved version of QUEUE.md the halted run could have read, so its premise never held; the real fault was the walkthrough heading not matching what the generated build view looks for, fixed at chat level; rejected keeping it and rewriting working steps to work around a formatting mismatch → 2026-08-26-afk-accounts-walkthrough.md
- [HASH] — [first-deploy-checks] retired into [afk-wire-up]: both unprovable checks written into the deploy walkthrough — step 5 now names a red error on the bundled-page import as the sign the entry-file split broke, step 6 names printing the four commands back as success; failed the keep-check's second limb as QUEUE.md-only, so it was planning work rather than a build; claims verified against `src/index.js`, `wrangler.toml` and `scripts/register-commands.mjs` → 2026-08-26-first-deploy-checks.md
- [HASH] — [kv-read-staleness] merged into [sign-page-browser-check]: the staleness question, the sixty-second ceiling, whose judgement it is, and the defeated shorter-poll fix all rewritten into the host item, whose Acceptance line was reworded to stop citing the retired slug; deferred once already for the same undescribable-build reason, so a second skip was refused → 2026-08-26-kv-read-staleness.md
- ac4cbc2 — Build chat overview: four cleared items shipped and the run halted on [afk-accounts], which is user work carrying no walkthrough, so no steps existed to hand over; records the build-file discipline slip where two items' work was noted only as index candidates and not under `Changes:`, caught and fixed at the close; per-item entries hold the reasoning → 2026-08-25-build.md
- ac4cbc2 — [worker-test-suite] built: Worker logic moved to `src/worker.js` taking the page HTML as a parameter so plain Node can load it, `src/index.js` reduced to the import plus wiring, `test/worker.test.mjs` added with 24 passing tests over parsing, DST, zone names, sign ids, the Ed25519 401 and all four commands against a Map-backed fake KV; the DST test found a real bug — `nextOccurrence` used now+24h and so set the sign a day late across a spring-forward — fixed in scope on the user's deferral to Claude; the wrapper itself is unbuilt until a real `wrangler` run → 2026-08-25-worker-test-suite-build.md
- ac4cbc2 — [register-slash-commands] built: `scripts/register-commands.mjs` PUTs the four definitions to Discord's global endpoint reading both credentials from the environment, plus an `npm run register` script; ticked UNCONFIRMED — only the missing-variable path was run, the success path waits on [afk-accounts] → 2026-08-25-register-slash-commands-build.md
- ac4cbc2 — [timezone-abbreviation] built: `formatInZone` now formats the clock for `en-US` and looks the zone name up separately across `en-US`/`en-GB`/`en-AU`, taking the first that gives letters and falling back to the offset form; checked against London BST, New York EDT, Sydney AEST and Kolkata GMT+5:30 → 2026-08-25-timezone-abbreviation-build.md
- ac4cbc2 — [ignore-build-dirs] built: `.gitignore` gained `node_modules/` and `.wrangler/` before either folder exists, verified with `git check-ignore` rather than by creating them → 2026-08-25-ignore-build-dirs-build.md
- 1cd6a89 — Planning chat overview: Unprocessed six entries down to one, Processed two items up to eight, cleared region one up to five; records the skill-invocation error, a duplicated step numbering caught and fixed in [afk-wire-up], [afk-accounts] moved off the top of the cleared run so a build run reaches the builds behind it, and the mid-session re-scan; per-item entries hold the reasoning → 2026-08-25-plan.md
- 1cd6a89 — [afk-wire-up] deploy walkthrough rewritten to ten checked steps: corrects the rough version's instruction to store the public key and bot token as Worker secrets (the key is a plain var in `wrangler.toml`, the token is unused by the Worker), adds the missing KV namespace creation with the `kv namespace` spelling confirmed against Cloudflare's docs, and inserts opening the sign page before the first `/afk` because that first call is refused until the page reports a timezone; `Blocked by:` corrected to drop shipped [discord-bot] and add [register-slash-commands] → 2026-08-25-afk-wire-up.md
- 1cd6a89 — [worker-test-suite] kept and cleared: project has no test file or test command, the `src/index.js` HTML text-module import blocks plain Node from loading it, so the item splits logic into `src/worker.js` and adds `test/worker.test.mjs` under Node's built-in runner with a Map-backed fake KV; Cloudflare's Vitest plugin weighed and refused on dependency cost with the revisit condition recorded → 2026-08-25-worker-test-suite.md
- 1cd6a89 — [register-slash-commands] filed and cleared after finding nothing in the project registers the four commands with Discord — deploy would succeed and `/` offer nothing; script reads `DISCORD_TOKEN` and `DISCORD_APPLICATION_ID` from the environment so the token never reaches the repo, registering from inside the Worker refused; [afk-wire-up] gained it as a blocker → 2026-08-25-register-slash-commands.md
- 1cd6a89 — [kv-read-staleness] deferred to the bottom of Unprocessed as undescribable-until-measured, with [sign-page-browser-check] amended to record how many seconds the page actually takes to flip after `/afk` and `/back` → 2026-08-25-kv-read-staleness.md
- 1cd6a89 — [sign-crossfade-eyeball] created as the user-owned half of the sign check, with a walkthrough for the twenty-minute photo cycle and an explicit "nothing observable in the project's files" note; split reasoning lives in the sibling entry → 2026-08-25-sign-crossfade-eyeball.md
- 1cd6a89 — [sign-page-eyeball] split after the capability check found the browser tool could run the countdown checks: [sign-page-browser-check] created with a cleared red flag on the sign address (designed out by regenerating the address after the check), build block corrected to have the user type the Discord commands while Claude watches, and the storage-lag timing folded in → 2026-08-25-sign-page-browser-check.md
- 1cd6a89 — SPEC gained two sentences checked against the Worker's code first — throwing away a leaked sign address via `/newsign`, and the first `/afk` being refused until the sign page reports a timezone — and [spec-address-sentences] was deleted, its whole content having moved to SPEC → 2026-08-25-spec-address-sentences.md
- 1cd6a89 — [timezone-abbreviation] kept and cleared after testing `Intl` across six zones: no single locale covers every zone, so the build tries `en-US`/`en-GB`/`en-AU` and falls back to the offset form; zones with no abbreviation in the data answered the item's open question, and switching wholesale to `en-GB` was refused for breaking New York → 2026-08-25-timezone-abbreviation.md
- 1cd6a89 — [ignore-build-dirs] kept and cleared first in the run: `.gitignore` and the disk both checked, confirming neither build folder was ever committed so the work is preventive rather than a history rewrite → 2026-08-25-ignore-build-dirs.md
- 8b583c4 — Migration run onto plugin 1.20.0-test18: every scaffold file, folder and setting checked and found already present (docs, FAQ, INBOX, resources, both `.gitignore` lines, brevity output style), format epoch already current so no document conversions ran, CLAUDE.md's managed block identical to the template and free of retired wording, SPEC.md free of a "Project docs" section; only `.throughliner-version` changed; queue and SPEC untouched; notes that the standing forward-advisory contradicts the queue about the account item's walkthrough → 2026-08-25-setup.md
- f06bdc3 — Built the Cloudflare Worker carrying the Discord interactions endpoint (Ed25519 verification, 401 on failure), `/afk` `/back` and the two address commands with address-bearing replies kept private, and KV-backed per-person state keyed on the sign id to halve the poll cost; `afk-sign_1.html` converted from a manual time form into a bot-driven page that polls its own state, reports its browser timezone and counts up past the return time, losing the green "I'm back!" state; tick is UNCONFIRMED pending a browser look after deploy; KV free-tier limits and its 60-second read staleness recorded → 2026-08-24-discord-bot.md
- 4f5c86b — Planning: sign moved from each person's machine to a hosted page with one private random address each, on Cloudflare Workers (research filed on Discord's signed HTTP interactions); SPEC lost its "no database" principle and gained private-address and own-timezone sentences; [discord-bot] rewritten with a build block and split into [afk-accounts] and [afk-wire-up]; [multi-user] and [themes] deleted; red flag on address guessability cleared by design → 2026-08-24-plan.md
- 4f5c86b — Setup adopted the AFK-cats folder: SPEC.md written describing a Discord bot (`/afk 3pm`, `/back`) that flips a local full-screen AFK sign on and off, with the countdown counting up once late; three captures filed and the existing afk-sign_1.html left untouched → 2026-08-24-setup.md
