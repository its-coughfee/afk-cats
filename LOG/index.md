# LOG Index

One-line summaries of each session. Newest first. Each line names the session's
full entry file in this folder.

- [HASH] — Planning chat overview: Unprocessed six entries down to one, Processed two items up to eight, cleared region one up to five; records the skill-invocation error, a duplicated step numbering caught and fixed in [afk-wire-up], [afk-accounts] moved off the top of the cleared run so a build run reaches the builds behind it, and the mid-session re-scan; per-item entries hold the reasoning → 2026-08-25-plan.md
- [HASH] — [afk-wire-up] deploy walkthrough rewritten to ten checked steps: corrects the rough version's instruction to store the public key and bot token as Worker secrets (the key is a plain var in `wrangler.toml`, the token is unused by the Worker), adds the missing KV namespace creation with the `kv namespace` spelling confirmed against Cloudflare's docs, and inserts opening the sign page before the first `/afk` because that first call is refused until the page reports a timezone; `Blocked by:` corrected to drop shipped [discord-bot] and add [register-slash-commands] → 2026-08-25-afk-wire-up.md
- [HASH] — [worker-test-suite] kept and cleared: project has no test file or test command, the `src/index.js` HTML text-module import blocks plain Node from loading it, so the item splits logic into `src/worker.js` and adds `test/worker.test.mjs` under Node's built-in runner with a Map-backed fake KV; Cloudflare's Vitest plugin weighed and refused on dependency cost with the revisit condition recorded → 2026-08-25-worker-test-suite.md
- [HASH] — [register-slash-commands] filed and cleared after finding nothing in the project registers the four commands with Discord — deploy would succeed and `/` offer nothing; script reads `DISCORD_TOKEN` and `DISCORD_APPLICATION_ID` from the environment so the token never reaches the repo, registering from inside the Worker refused; [afk-wire-up] gained it as a blocker → 2026-08-25-register-slash-commands.md
- [HASH] — [kv-read-staleness] deferred to the bottom of Unprocessed as undescribable-until-measured, with [sign-page-browser-check] amended to record how many seconds the page actually takes to flip after `/afk` and `/back` → 2026-08-25-kv-read-staleness.md
- [HASH] — [sign-crossfade-eyeball] created as the user-owned half of the sign check, with a walkthrough for the twenty-minute photo cycle and an explicit "nothing observable in the project's files" note; split reasoning lives in the sibling entry → 2026-08-25-sign-crossfade-eyeball.md
- [HASH] — [sign-page-eyeball] split after the capability check found the browser tool could run the countdown checks: [sign-page-browser-check] created with a cleared red flag on the sign address (designed out by regenerating the address after the check), build block corrected to have the user type the Discord commands while Claude watches, and the storage-lag timing folded in → 2026-08-25-sign-page-browser-check.md
- [HASH] — SPEC gained two sentences checked against the Worker's code first — throwing away a leaked sign address via `/newsign`, and the first `/afk` being refused until the sign page reports a timezone — and [spec-address-sentences] was deleted, its whole content having moved to SPEC → 2026-08-25-spec-address-sentences.md
- [HASH] — [timezone-abbreviation] kept and cleared after testing `Intl` across six zones: no single locale covers every zone, so the build tries `en-US`/`en-GB`/`en-AU` and falls back to the offset form; zones with no abbreviation in the data answered the item's open question, and switching wholesale to `en-GB` was refused for breaking New York → 2026-08-25-timezone-abbreviation.md
- [HASH] — [ignore-build-dirs] kept and cleared first in the run: `.gitignore` and the disk both checked, confirming neither build folder was ever committed so the work is preventive rather than a history rewrite → 2026-08-25-ignore-build-dirs.md
- 8b583c4 — Migration run onto plugin 1.20.0-test18: every scaffold file, folder and setting checked and found already present (docs, FAQ, INBOX, resources, both `.gitignore` lines, brevity output style), format epoch already current so no document conversions ran, CLAUDE.md's managed block identical to the template and free of retired wording, SPEC.md free of a "Project docs" section; only `.throughliner-version` changed; queue and SPEC untouched; notes that the standing forward-advisory contradicts the queue about the account item's walkthrough → 2026-08-25-setup.md
- f06bdc3 — Built the Cloudflare Worker carrying the Discord interactions endpoint (Ed25519 verification, 401 on failure), `/afk` `/back` and the two address commands with address-bearing replies kept private, and KV-backed per-person state keyed on the sign id to halve the poll cost; `afk-sign_1.html` converted from a manual time form into a bot-driven page that polls its own state, reports its browser timezone and counts up past the return time, losing the green "I'm back!" state; tick is UNCONFIRMED pending a browser look after deploy; KV free-tier limits and its 60-second read staleness recorded → 2026-08-24-discord-bot.md
- 4f5c86b — Planning: sign moved from each person's machine to a hosted page with one private random address each, on Cloudflare Workers (research filed on Discord's signed HTTP interactions); SPEC lost its "no database" principle and gained private-address and own-timezone sentences; [discord-bot] rewritten with a build block and split into [afk-accounts] and [afk-wire-up]; [multi-user] and [themes] deleted; red flag on address guessability cleared by design → 2026-08-24-plan.md
- 4f5c86b — Setup adopted the AFK-cats folder: SPEC.md written describing a Discord bot (`/afk 3pm`, `/back`) that flips a local full-screen AFK sign on and off, with the countdown counting up once late; three captures filed and the existing afk-sign_1.html left untouched → 2026-08-24-setup.md
