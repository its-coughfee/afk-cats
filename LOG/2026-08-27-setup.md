# 23cb4e8 — Plugin top-up from 1.20.0-test18 to 1.21.0-test3: the only gap was a new Visibility section in CLAUDE.md's managed block, filled in from the repository's own state

A setup run on an already-adopted project, so it was a top-up rather than an
adoption: the version marker read 1.20.0-test18 against an installed
1.21.0-test3. The document-format number was already 4, matching the installed
plugin, so no document conversions were owed and none ran.

Every scaffold check came back present — SPEC.md, QUEUE.md, LOG/, FAQ/, INBOX/
with its archive, resources/research/ — and every setting attached to them was
already made: `.gitignore` carries both `INBOX/` and `.throughliner/`, and the
project's own settings already select the brevity output style. No REGISTRY.md
exists, the queue's two section preambles are already blockquotes, and the
project's CLAUDE.md carries none of the retired terms the migration searches for.
SPEC.md carries no "Project docs" section, so there was nothing stale to report
there either.

The single real difference was in the plugin-managed block of the project's
CLAUDE.md: 1.21.0-test3's template adds a **Visibility** section, recording which
repository holds the project's documents and whether they are published. The
block was otherwise identical to the template line for line, and held no
user-authored text, so nothing had to be moved below the end marker — the section
was inserted ahead of Language and the rest left untouched.

Filling that line in was Claude's call rather than a question put to the user,
and the reason is that the answer was readable off the folder rather than a
decision anyone still had to make: `.gitignore` ignores none of the three
documents, so SPEC.md, QUEUE.md and LOG/ are all tracked, and `git remote` shows
no remote, so the repository has no online home and nothing is published. The
alternative considered and rejected was leaving the line blank as the template
ships it — blank is what the template means by "not settled yet", and this
project's visibility is settled and observable, so a blank line would have
understated what is known.

**Queue changes:** none. No captures filed, no items processed, no reordering,
and the cleared-to-run marker stays at the top of Processed where the previous
session left it — this run made no disposition on anything, which is planning
work.

**Work processed:** none.

Advisory: not needed — the advisory filed at the previous close, pointing at
[wire-up-blocker-unresolvable], is still the first thing in Unprocessed and no
planning run has read it since. Replacing it would have swapped a correct note
for the same note.

**Also in this chat:** nothing beyond the setup run itself.
